name: 🏗️ Сборка .exe и выпуск

# Запуск только при создании тега вида v*
on:
  push:
    tags:
      - 'v*'
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-exe:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ['3.11']

    steps:
      - name: 🔹 Checkout кода
        uses: actions/checkout@v4

      - name: 🔹 Установка Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: 🔹 Чтение версии из файла VERSION
        id: version
        run: |
          VERSION=$(cat VERSION)
          echo "RELEASE_VERSION=$VERSION" >> $GITHUB_ENV
          echo "TAG_NAME=v$VERSION" >> $GITHUB_ENV
        shell: bash

      - name: 🔹 Лог информация о версии
        run: |
          echo "🔄 Сборка версии: ${{ env.RELEASE_VERSION }}"
          echo "🏷  Тег: ${{ env.TAG_NAME }}"

      - name: 🔹 Установка зависимостей
        timeout-minutes: 10
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -e .
          pip install --no-cache-dir PyQt6 pyinstaller toml

      - name: 🔹 Сборка .exe с помощью PyInstaller
        run: |
          python -m PyInstaller   --noconsole   --onefile   --name "ConverterCSVtoRDF"   --add-data "config.json;."   --distpath "dist"   src/ui.py

      - name: 🔹 Подготовка финальной папки
        run: |
          mkdir dist/final
          Copy-Item -Path "config.json" -Destination "dist/final/config.json"
          Copy-Item -Path "dist/ConverterCSVtoRDF.exe" -Destination "dist/final/ConverterCSVtoRDF.exe"
        shell: pwsh

      - name: 🔹 Архивирование дистрибутива
        run: |
          cd dist/final
          Compress-Archive -Path * -DestinationPath ../converter-package.zip
        shell: pwsh

      - name: 🔹 Извлечь изменения для версии из CHANGELOG.md
        id: changelog
        run: |
          $version = "${{ env.RELEASE_VERSION }}"
          $content = Get-Content -Path CHANGELOG.md -Raw
          $regex = "(?s)(?<=## \[$version\][^\n]*\n)(.*?)(?=\n## |\Z)"
          $changes = [regex]::Match($content, $regex).Value.Trim()
          if ([string]::IsNullOrWhiteSpace($changes)) {
            $changes = "Обновления не задокументированы."
          }
          $changes = $changes -replace '^', '            '
          echo "CHANGELOG_BLOCK=$changes" >> $env:GITHUB_ENV
        shell: pwsh

      - name: 🔹 Создание релиза на GitHub
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: Выпуск ${{ env.TAG_NAME }}
          body: |
            ## 🚀 Выпуск ${{ env.TAG_NAME }}

            Автоматически собранная версия приложения **Конвертер CSV → RDF/XML**.

            ### 📦 Что включено
            - `ConverterCSVtoRDF.exe` — основная программа
            - `config.json` — конфигурация (должен быть в той же папке)

            ### 📝 Что нового
            ${{ env.CHANGELOG_BLOCK }}

            ### 🧰 Как использовать
            1. Скачайте `converter-package.zip`
            2. Распакуйте в любую папку
            3. Запустите `ConverterCSVtoRDF.exe`
            4. Убедитесь, что `config.json` находится рядом с `.exe`

            Версия: ${{ env.RELEASE_VERSION }}
          draft: false
          prerelease: false
          files: dist/converter-package.zip
          overwrite: true
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
