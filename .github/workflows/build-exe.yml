name: 🏗️ Сборка .exe и выпуск

on:
  push:
    branches: ['main', 'master']

jobs:
  build-exe:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ['3.11']

    steps:
      - name: 🔹 Checkout кода
        uses: actions/checkout@v4

      - name: 🔹 Установка Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: 🔹 Установка зависимостей
        timeout-minutes: 10
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -e .
          pip install --no-cache-dir PyQt6 pyinstaller toml

      - name: 🔹 Сборка .exe с помощью PyInstaller
        run: |
          python -m PyInstaller   --noconsole   --onefile   --name "ConverterCSVtoRDF"   --add-data "config.json;."   --distpath "dist"   src/ui.py

      - name: 🔹 Подготовка финальной папки
        run: |
          mkdir dist/final
          Copy-Item -Path "config.json" -Destination "dist/final/config.json"
          Copy-Item -Path "dist/ConverterCSVtoRDF.exe" -Destination "dist/final/ConverterCSVtoRDF.exe"
        shell: pwsh

      - name: 🔹 Архивирование дистрибутива
        run: |
          cd dist/final
          Compress-Archive -Path * -DestinationPath ../converter-package.zip
        shell: pwsh

      - name: 🔹 Создание релиза и загрузка артефакта
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: v${{ env.RELEASE_VERSION }}
          name: Выпуск v${{ env.RELEASE_VERSION }}
          body: |
            Автоматически собранный дистрибутив:
            - `ConverterCSVtoRDF.exe`
            - `config.json`

            Запустите `.exe` — он будет искать `config.json` в той же папке.
          draft: false
          prerelease: false
          files: dist/converter-package.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_VERSION: ${{ github.run_number }}
