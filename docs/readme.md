# 🚀 Конвертер CSV → RDF/XML (CIM16)

Программа для преобразования иерархических данных из CSV-файлов в формат RDF/XML согласно стандарту **CIM (Common Information Model)**.  
Построена на основе **собственного фреймворка `monitel_framework`**, обеспечивающего модульность, логирование, стиль и готовность к развёртыванию.

Поддерживает:

- Виртуальные контейнеры (объекты с UID не создаются, но используются как родители)
- Русскоязычные заголовки (настраивается в `config.json`)
- Кодировки: UTF-8, cp1251, UTF-8-sig
- Пакетную обработку
- Современный GUI с тёмной темой
- Логирование в реальном времени (в окно и в файлы)
- Готовность к сборке в однофайловый `.exe`

---

## 📦 Возможности

- ✅ Преобразование CSV → RDF/XML (CIM16)
- ✅ Поддержка **виртуальных контейнеров** (родительские объекты без UID)
- ✅ Чтение **русскоязычных заголовков** (настраивается в `config.json`)
- ✅ Гибкая **конфигурация через JSON**
- ✅Графический интерфейс с тёмной темой (PyQt6)
- ✅ Логирование в файл и в GUI (с уровнем `DEBUG`)
- ✅ Поддержка кодировок: `UTF-8`, `cp1251`, `UTF-8-sig`
- ✅ Пакетная обработка нескольких CSV-файлов
- ✅ Автоматическое создание папки `log/`
- ✅ Умная обработка ККС:
  - `AssetContainer`: `<me:IdentifiedObject.mRIDStr>ККС</me:IdentifiedObject.mRIDStr>`
  - `GenericPSR`: `<rh:PowerSystemResource.ccsCode>ККС</rh:PowerSystemResource.ccsCode>`
- ✅ Готов к сборке в **однофайловый `.exe`**
- ✅ Расширяемый **фреймворк `monitel_framework`** для других проектов

---

## 🛠 Установка и запуск

### Вариант 1: Запуск из исходников (разработка)

```bash
# Клонировать репозиторий
git clone https://github.com/yourname/csv-to-rdf-converter.git
cd csv-to-rdf-converter
# Создать виртуальное окружение
python -m venv venv-ui
venv-ui\Scripts\activate  # Windows
# source venv-ui/bin/activate  # Linux/Mac
# Установить зависимости
pip install PyQt6 toml
# Запустить GUI
python src/ui.py
```

---

Вариант 2: Запуск из .exe (для пользователей)

1. Скачайте архив с программой.
2. Распакуйте все файлы в одну папку.
3. Запустите `Конвертер CSV-RDF.exe`.
4. Убедитесь, что рядом лежит `config.json`.
   > ⚠️ При первом запуске программа создаст папку `log/` (если указано в конфиге).

---

📁 Структура проекта

```
csv-to-rdf-converter/
│
├── config.json                       ← Основная конфигурация
├── build.toml                        ← Конфиг сборки .exe
├── build.py                          ← Скрипт сборки через PyInstaller
├── README.md                         ← Эта инструкция
│
├── src/
│   ├── ui.py                         ← Графический интерфейс (наследует BaseMainWindow)
│   ├── main.py                       ← Основная логика обработки
│   │
│   └── monitel_framework/            ← Общий фреймворк
│       ├── __init__.py               ← Экспорт классов
│       ├── config.py                 ← ConfigManager
│       ├── files.py                  ← FileManager
│       ├── logging.py                ← Система логирования
│       ├── ui_base.py                ← Базовое окно GUI
│       └── utils.py                  ← Вспомогательные функции
│
├── modules/
│   ├── hierarchy_parser.py           ← Парсинг CSV → иерархия
│   └── xml_generator.py              ← Генерация RDF/XML
│
├── examples/
│   └── sample.csv                    ← Пример входного файла
│
├── log/                              ← Автоматически: логи (создаётся при запуске)
├── dist/                             ← Готовый .exe (создаётся при сборке)
└── build/                            ← Временная папка PyInstaller
```

---

## ⚙️ Конфигурация (`config.json`)

### `logging`

| Параметр      | Описание                                                                   |
| ------------- | -------------------------------------------------------------------------- |
| `level`       | Уровень логирования:`DEBUG`,`INFO`,`WARNING`,`ERROR`                       |
| `format`      | Формат сообщения (поддерживает`%(asctime)s`,`%(levelname)s`,`%(message)s`) |
| `date_format` | Формат даты (по умолчанию:`%Y-%m-%d %H:%M:%S`)                             |

> 🔹 Рекомендуется `DEBUG` при разработке, `INFO` — в продакшене.

### `io`

| Параметр        | Описание                                                        |
| --------------- | --------------------------------------------------------------- |
| `log_dir`       | Папка для логов (по умолчанию:`log`)                            |
| `exclude_files` | Список файлов, которые игнорируются (например, `["Sample.csv"]` |

### `csv_headers`

| Параметр   | Описание                                                                   |
| ---------- | -------------------------------------------------------------------------- |
| `path`     | Название столбца с путём (например,`"НаименованиеКонтейнераОборудования"`) |
| `uid`      | Столбец с UID виртуального контейнера                                      |
| `CCK_code` | Столбец с кодом ККС (теперь используется по-новому)                        |

---

## 📄 Формат CSV

Пример содержимого `sample.csv`:

| НаименованиеКонтейнераОборудования | uid                                  | ОбъектРемонтаКодККС |
| ---------------------------------- | ------------------------------------ | ------------------- |
| ГРЩ                                | 123e4567-e89b-12d3-a456-426614174000 |                     |
| ГРЩ\ЩО                             |                                      |                     |
| ГРЩ\ЩО\ЩО-1                        |                                      | ККС-001             |

> ⚠️ Разделитель: ; или , — определяется автоматически
> ✅ Поддерживаемые кодировки: UTF-8, cp1251, UTF-8-sig.

---

### 📋 Правила записи ККС (Код Классификатора Средств)

| Тип объекта в XML    | ТЕГ ДЛЯ ККС                                                              | Пример записи                                                              | Когда используется                                                                          |
| -------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `cim:AssetContainer` | `<me:IdentifiedObject.mRIDStr>{ККС}</me:IdentifiedObject.mRIDStr>`       | `<me:IdentifiedObject.mRIDStr>ККС-001</me:IdentifiedObject.mRIDStr>`       | Для контейнеров (групп, шкафов, подстанций), у которых есть дочерние объекты или это корень |
| `me:GenericPSR`      | `<rh:PowerSystemResource.ccsCode>{ККС}</rh:PowerSystemResource.ccsCode>` | `<rh:PowerSystemResource.ccsCode>ККС-002</rh:PowerSystemResource.ccsCode>` | Для конечных объектов (оборудования без детей — листьев дерева)                             |

#### 🔍 Пояснение:

- **`AssetContainer`**— используется для:
  - ГРЩ, ЩО, ПС, цехов и т.п.
  - Объектов, которые содержат в себе другое оборудование.
- **`GenericPSR`**— используется для:
  - Конечного оборудования: двигателей, автоматов, насосов и т.п.
  - Объектов, которые **не имеют подчинённых**.

---

## 🧰 Сборка `.exe`

Для создания исполняемого файла используется `PyInstaller`.

### Вариант 1: Сборка через отдельный `.py` файл\

#### 1. Установите зависимости

```bash
pip install pyinstaller toml
```

#### 2. Настройте `build.toml`

```toml
[pyinstaller]
script = "src/ui.py"
name = "Конвертер CSV-RDF"
windowed = true
onefile = true

datas = [
    { src = "config.json", dest = "." }
]

hiddenimports = [
    "PyQt6",
    "PyQt6.QtWidgets",
    "PyQt6.QtCore",
    "PyQt6.QtGui",
    "monitel_framework.config",
    "monitel_framework.files",
    "monitel_framework.logging",
    "monitel_framework.ui_base"
]
```

#### 3. Запустите сборку

```bash
python build.py
```

> ✅ Готовый файл: `dist/Конвертер CSV-RDF.exe`

### Вариант 2: сборка через консольную команду

Запустите сборку

```bash
pyinstaller --noconsole --onefile --paths "путь\к\папке\src" src/ui.py
```

---

## 📋 Требования

- **Python 3.11+** (рекомендуется 64-bit)
- **PyQt6** — для GUI
- **toml** — для чтения `build.toml`
- **PyInstaller** — для сборки `.exe`

---

## 📂 Примеры

- Пример CSV: `examples/sample.csv`
- Логи: `log/gui_2025-09-05.log`, `log/sample_2025-09-05.log`

---

## 📞 Поддержка

**Monitel Team**  
📧 [gornovzena99@gmail.com](mailto:gornovzena99@gmail.com)  
🌐 [github.com/EvTKi/csv-to-rdf-converter](https://github.com/EvTKi/csv-to-rdf-converter)
